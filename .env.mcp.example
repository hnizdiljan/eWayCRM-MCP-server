# =============================================================================
# MCP Server Environment Variables Configuration
# =============================================================================
#
# POZNÁMKA: Tento soubor slouží pouze jako reference!
#
# ENV proměnné pro MCP server se nastavují v:
#   claude_desktop_config.json -> mcpServers -> eway-crm -> env
#
# MCP server načte všechny proměnné s prefixem MCP_API_ a odstraní prefix
# Příklad: MCP_API_EWAY_USERNAME se předá API jako EWAY_USERNAME
#
# =============================================================================

# =============================================================================
# eWay-CRM API Configuration
# =============================================================================

# Základní konfigurace (POVINNÉ)
MCP_API_EWAY_API_URL=https://trial.eway-crm.com/31994
MCP_API_EWAY_USERNAME=uzivatel@firma.cz

# =============================================================================
# Autentizace - VOLBA 1: Legacy Authentication (username + password hash)
# =============================================================================

# Hash hesla (získáte pomocí: npm run hash-password)
MCP_API_EWAY_PASSWORD_HASH=váš-hash-hesla

# =============================================================================
# Autentizace - VOLBA 2: OAuth2 Authentication (doporučeno)
# =============================================================================

# OAuth2 Client credentials
# MCP_API_OAUTH_CLIENT_ID=your-client-id
# MCP_API_OAUTH_CLIENT_SECRET=your-client-secret
# MCP_API_OAUTH_REDIRECT_URI=http://localhost:7777/api/v1/oauth2/callback

# =============================================================================
# Server Configuration
# =============================================================================

# Port na kterém běží REST API (výchozí: 7777)
MCP_API_PORT=7777

# Log level (debug, info, warn, error)
MCP_API_LOG_LEVEL=info

# Base URL serveru (pro OAuth2 callback)
MCP_API_SERVER_BASE_URL=http://localhost:7777

# =============================================================================
# Application Configuration
# =============================================================================

# Environment (development, production)
NODE_ENV=production

# Client Machine ID (automaticky generováno)
# MCP_API_CLIENT_MACHINE_ID=custom-machine-id

# Client Machine Name (automaticky detekováno)
# MCP_API_CLIENT_MACHINE_NAME=My-Computer

# =============================================================================
# POUŽITÍ V CLAUDE DESKTOP CONFIG
# =============================================================================
#
# Přidejte tyto proměnné do claude_desktop_config.json:
#
# {
#   "mcpServers": {
#     "eway-crm": {
#       "command": "node",
#       "args": ["C:\\cesta\\k\\dist\\mcp-server.js"],
#       "env": {
#         "NODE_ENV": "production",
#         "MCP_API_EWAY_USERNAME": "uzivatel@firma.cz",
#         "MCP_API_EWAY_PASSWORD_HASH": "hash-hesla",
#         "MCP_API_EWAY_API_URL": "https://trial.eway-crm.com/31994",
#         "MCP_API_PORT": "7777",
#         "MCP_API_LOG_LEVEL": "info"
#       }
#     }
#   }
# }
#
# =============================================================================
